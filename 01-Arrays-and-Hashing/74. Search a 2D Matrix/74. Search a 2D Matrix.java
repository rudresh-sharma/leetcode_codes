    static boolean binSearch(int[] arr, int n, int target){        int low = 0;        int high = n-1;        while(low<=high){        return false;    }            }        }            if(isFound){                return true;            int n = a.length;            boolean isFound = binSearch(a, n, target);    public boolean searchMatrix(int[][] matrix, int target) {        for(int i=0; i<matrix.length; i++){            int[] a = matrix[i];class Solution {